```env
# Server Environment Variables
# MongoDB connection string (replace with your MongoDB Atlas or local MongoDB URI)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT secret for signing tokens (replace with a secure random string)
JWT_SECRET=your-secret-key

# Server port
PORT=5000

# Client Environment Variables
# API base URL for the frontend to communicate with the backend
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
```